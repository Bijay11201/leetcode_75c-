class Solution {
public:
    int largestAltitude(vector<int>& gain) {
     int sum=0;
     int maximum=0;
     for(int i=0;i<gain.size();i++){
        sum+=gain[i];
        maximum=max(maximum,sum);
     }
     return maximum;
    }
};